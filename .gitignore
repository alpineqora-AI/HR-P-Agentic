# Java / Gradle
backend/build/
backend/.gradle/

# Node / frontend
frontend/node_modules/
frontend/dist/

# Candidate career app (TalentBridge): only the career frontend is integrated
# ("career site only"). Its original backend is not used here, and build/deps
# are not tracked.
Careers and Conv/backend/
Careers and Conv/**/node_modules/
Careers and Conv/**/dist/

# Local theme overrides
frontend/src/styles/*.local.css

# Secrets / local config
.env
.env.local

# Source artifacts that shouldn't be tracked
recruiting_feature_matrix_*.xlsx

# misc
.DS_Store
*.log
